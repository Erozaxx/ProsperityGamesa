'use strict';

angular.module('prosperity')
    .service('Tinkery', ['$rootScope',
        function Tinkery($rootScope) {
            // AngularJS will instantiate a singleton by calling "new" on this function
            var tinkery = {
                
            }

            return tinkery;
        }
    ]);